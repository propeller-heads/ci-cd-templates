name: Release

on:
  workflow_call:
    inputs:
      runs_on:
        required: false
        type: string
        default: ubuntu-latest

jobs:
  deploy:
    runs-on: '${{ inputs.runs_on }}'
    steps:
      - name: "Deployment"
        run: | 
          echo "Deployment"
