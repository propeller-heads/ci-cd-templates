name: Release

on:
  workflow_call:
    inputs:
      runs_on:
        required: false
        type: string
        default: ubuntu-latest

jobs:
  build-and-push:
    runs-on: '${{ inputs.runs_on }}'
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: 'Build and push image to GAR'
        run: |
            echo "Build and push"
